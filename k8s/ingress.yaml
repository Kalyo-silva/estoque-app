apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: estoque-ingress
  namespace: estoque-app
spec:
  ingressClassName: nginx
  rules:
    - host: estoque.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend-service
                port:
                  number: 80
